# Android / Gradle build
*.iml
.gradle/
/build/
/app/build/
/captures/
.externalNativeBuild/
.cxx/
build/
local.properties

# IDE / tooling
.idea/
*.hprof
.claude/

# Python tooling (scripts/)
__pycache__/
*.pyc

# Bundled libretro cores are GPL binaries fetched from the buildbot, not committed.
# Run scripts/fetch_cores.sh (or .ps1) to populate these.
/app/src/main/jniLibs/**/*.so

# Signing
*.keystore
*.jks

# Built artifacts
*.apk
*.aab

# BIOS / ROM files must never be committed (copyright)
*.bin
*.bios
*.rom
*.chd
*.cue
*.iso
*.pbp
*.sbi
*.nds
*.gba
*.zip
!gradle/wrapper/gradle-wrapper.jar

# OS
.DS_Store
Thumbs.db

# ---------------------------------------------------------------------------
# NEVER commit games or firmware. ROMs, disc images and BIOS/boot files are
# copyrighted; this repo is public. Keep them on the device or outside the tree.
# ---------------------------------------------------------------------------
*.nes
*.fds
*.sfc
*.smc
*.gb
*.gbc
*.gba
*.n64
*.z64
*.v64
# NOTE: '*.md' is deliberately NOT ignored — it would hide our Markdown docs.
# Mega Drive .md ROMs must simply never be placed in the repo.
*.gen
*.smd
*.sms
*.gg
*.32x
*.pce
*.sgx
*.ngp
*.ngc
*.ws
*.wsc
*.int
*.col
*.vec
*.min
*.a26
*.a52
*.a78
*.lnx
*.d64
*.t64
*.prg
*.crt
*.adf
*.hdf
*.tap
*.tzx
*.z80
*.cdt
*.sna
*.mx1
*.mx2
*.dsk
*.chd
*.cue
*.gdi
*.cdi
*.iso
*.img
*.mdf
*.mds
*.nrg
*.pbp
*.cso
*.ecm
*.bin
*.7z
roms/
bios/
system/
*bios*.zip
scph*

# Release signing — never commit
keystore.properties
*.jks
*.keystore

# GPL core binaries that rode in with the vendored fork demo app (never built here)
libretrodroid/app/src/main/jniLibs/
